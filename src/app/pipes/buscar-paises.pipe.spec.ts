import { BuscarPaisesPipe } from './buscar-paises.pipe';

describe('BuscarPaisesPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPaisesPipe();
    expect(pipe).toBeTruthy();
  });
});
